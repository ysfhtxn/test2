name: AutoCopy

on:
  push:
    branches: dd


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      #- name: Install mustache (to update the date)
       # run:  apk add ruby && gem install mustache
      - name:  copy file gotten
        run: |
          git checkout dd
          cat .build_date.md > date.md
          git checkout main -- date.md
